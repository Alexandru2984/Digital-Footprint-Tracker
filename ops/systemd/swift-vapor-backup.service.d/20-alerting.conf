# A backup that fails in silence is the same as no backup. The job runs once a
# day at 02:00 UTC with nobody watching, so route its failures to the operator.
[Unit]
OnFailure=swift-vapor-alert@%n.service
