[Service]
# One-time empty-database bootstrap only. Remove the installed drop-in and
# encrypted credential as soon as the migration unit creates the administrator.
LoadCredentialEncrypted=admin-password:/etc/credstore.encrypted/swift-vapor-admin-bootstrap-password
Environment=ADMIN_PASSWORD_FILE=%d/admin-password
